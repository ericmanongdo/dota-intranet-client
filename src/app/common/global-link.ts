export interface GlobalLink {
    id?: number
    title: string
    url: string
    textColor: string
    backgroundColor: string
    active: number
    categoryId: number
    categoryName?: string
}
